name: SEO Checks

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  seo-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run SEO lint
        run: ./scripts/seo_lint.sh
